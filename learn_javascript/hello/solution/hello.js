function hello(name) {
	if (name) {
		return "Hello, " + name + "!"
	} else {
		return "Hello!"
	}
	// if (typeof(name) == 'undefined') {
	// 	return "Hello!"
	// } else {
	// 	return "Hello, " + name + "!"
	// }
}
